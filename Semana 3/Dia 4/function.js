function speedup(velocity, acceleration) {
    let newVelocity = velocity + acceleration;
    console.log('Nova velocidade' + newVelocity);
}

speedup(20, 10)











function seepdUp(velocity, acceleration) {
    let newVelocity = velocity + acceleration
    console.log('Nova velocidade' + newVelocity)
    return newVelocity
}

console.log(speedup(200, 50))

speedup(20, 10)